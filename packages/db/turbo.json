{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "db:generate": {
      "env": ["DATABASE_MIGRATION_URL"]
    },
    "db:migrate": {
      "env": ["DATABASE_MIGRATION_URL"]
    },
    "test": {
      "cache": false,
      "env": ["TEST_POSTGRES_SUPERUSER_URL"]
    }
  }
}
