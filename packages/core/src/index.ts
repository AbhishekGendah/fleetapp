// Pure domain logic (money, dates, ledger, matching) lands here in a later
// task. Empty for now — this package exists so the test pipeline is wired
// up before there's real logic to test.
export {};
